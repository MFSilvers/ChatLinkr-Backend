{
  "name": "chatlinkr-backend",
  "version": "1.0.0",
  "description": "ChatLinkr Backend with PHP API and WebSocket server",
  "scripts": {
    "start": "./start.sh",
    "install-websocket": "cd websocket && npm install"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "socket.io": "^4.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
